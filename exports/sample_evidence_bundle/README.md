# Sample evidence bundle

This directory contains a machine-readable sample deliverable representing the underlying proof package that supports the human-readable audit pack.

## Included files

- `manifest.json` — bundle metadata and file inventory
- `provenance.jsonl` — simplified provenance chain excerpt
- `kg_delta.jsonl` — example knowledge-graph delta records
