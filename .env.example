# Required for Docker: the server refuses to bind to 0.0.0.0 without a token.
# Set this to a long random value.
FG_API_TOKEN=change-me

# Optional safety knobs
FG_MAX_UPLOAD_BYTES=0
FG_CMD_TIMEOUT_S=600
FG_WORKER_POLL=1.0
